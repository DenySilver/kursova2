<!DOCTYPE html>
<html lang="uk">
<head>
    <meta charset="UTF-8">
    <title>LanguageApp</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
    <style> .hover-effect:hover { transform: translateY(-5px); transition: 0.3s; cursor: pointer; } </style>
</head>
<body class="bg-light">
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
        <div class="container">
            <a class="navbar-brand" href="/dashboard">LanguageApp 🦉</a>
            <div class="d-flex align-items-center gap-3">
                <% if(currentUser) { %>
                    <a href="/profile" class="text-white text-decoration-none">
                        <img src="<%= currentUser.avatar_url %>" class="rounded-circle border border-white" width="35" height="35">
                        <span class="ms-2"><%= currentUser.username %></span>
                    </a>
                    <a href="/auth/logout" class="btn btn-sm btn-outline-light">Вихід</a>
                <% } else { %>
                    <a href="/auth/login" class="btn btn-sm btn-primary">Вхід</a>
                <% } %>
            </div>
        </div>
    </nav>