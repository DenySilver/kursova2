<%- include('head') %>

<body class="bg-light">

<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4 sticky-top">
    <div class="container">
        <a class="navbar-brand fw-bold" href="<%= typeof userId != 'undefined' ? '/user/' + userId + '/dashboard' : '/' %>">
            🦉 LanguageApp
        </a>
        
        <div class="collapse navbar-collapse" id="navbarNav">
            <ul class="navbar-nav me-auto">
                <% if(typeof user != 'undefined' && user.is_admin) { %>
                    <li class="nav-item">
                        <a class="btn btn-warning btn-sm fw-bold ms-2 text-dark" href="/admin">
                            🛠 Адмін-панель
                        </a>
                    </li>
                <% } %>
            </ul>

            <div class="navbar-nav ms-auto d-flex align-items-center gap-3">
                <% if(typeof userId != 'undefined') { %>
                    <a href="/user/<%= userId %>/dashboard" class="btn btn-outline-info text-white border-0 fw-bold">Вивчити мову</a>
                    <a href="/user/<%= userId %>" class="nav-link text-white fw-bold">Мій Профіль</a>
                    <a href="/auth/logout" class="btn btn-sm btn-danger px-3">Вихід</a>
                <% } else { %>
                    <a href="/auth/login" class="btn btn-sm btn-primary px-3">Вхід</a>
                <% } %>
            </div>
        </div>
    </div>
</nav>