<!DOCTYPE html>
<html lang="uk">
<head>
    <title>Вхід</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
</head>
<body class="container py-5 text-center">
    <h1>Ласкаво просимо! 👋</h1>
    <p class="mb-4">Оберіть користувача для входу (Демо режим):</p>
    <div class="d-flex justify-content-center gap-3">
        <% users.forEach(u => { %>
            <a href="/user/<%= u.user_id %>" class="card p-3 text-decoration-none text-dark shadow-sm" style="width: 200px;">
                <img src="<%= u.avatar_url %>" class="rounded-circle mx-auto mb-2" width="60">
                <h5><%= u.username %></h5>
            </a>
        <% }) %>
    </div>
</body>
</html>